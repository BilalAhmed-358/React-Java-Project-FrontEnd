import React from 'react'
import Hero from '../hero/Hero'

const Home = ({ movies }) => {
    // if (movies !== undefined) {

    return (
        <Hero movies={movies} />

    )

}

export default Home
